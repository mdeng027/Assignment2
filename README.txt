Unfortunately, once the login ID implementation was added, a client connects and immediately gets terminated because it cannot parse a login id argument.

Prior to that, Exercise 1 and 2 functioned as intended, and would have allowed more test cases to pass.
